package main

import (
	"bufio"
	"fmt"
	"os"
)

func main() {
    f, err := os.Open("4.in")
    if err != nil {
        fmt.Println(err)
        return
    }
    r := bufio.NewReader(f)
    for {
        line, err := r.ReadString('\n')
        if err != nil {
            break
        }
    }
}
